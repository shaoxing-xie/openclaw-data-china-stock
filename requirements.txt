# Core dependencies for tool_runner and core data tools.
# Versions are intentionally left unpinned to reduce install friction across environments.

pydantic
PyYAML
requests
numpy
pytz
pandas
pyarrow

# Data providers (used by data_collection tools)
akshare
tushare
yfinance
mootdx
efinance

# Optional: DuckDB for faster cache queries (improves performance for large historical datasets)
duckdb>=1.1.0

